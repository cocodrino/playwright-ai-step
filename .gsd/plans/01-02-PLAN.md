# Plan 01-02: Phase 2 — Integration Tests + Multi-Provider

## Phase Overview
Goal: ai() works end-to-end with real LLM calls. Verify on a real browser page.

## Prerequisites (before starting)
- Ollama Cloud API key configured in .env (OLLAMA_API_KEY, OLLAMA_BASE_URL, OLLAMA_MODEL)
- Playwright Chromium installed (done in Phase 1)

## Tasks

<task type="auto">
  <name>Task 1: Multi-Provider LLM Config (flexible switching)</name>
  <files>src/config.ts, src/llm-client.ts</files>
  <action>
    Refactor `src/config.ts` to support multiple LLM providers with a simple provider switch:
    
    ```
    LLM_PROVIDER=ollama       # → use OLLAMA_* vars
    LLM_PROVIDER=minimax      # → use MINIMAX_* vars
    LLM_PROVIDER=openai       # → use OPENAI_* vars
    ```
    
    The resolveLLMConfig() function should:
    1. Read LLM_PROVIDER env var (default: "ollama")
    2. Load provider-specific env vars
    3. Return LLMConfig with correct baseUrl, apiKey, model
    4. Auto-detect: if LLM_API_KEY is set but no LLM_PROVIDER, infer from baseUrl (localhost → ollama, api.minimax.io → minimax)
    
    Refactor src/llm-client.ts to use the resolved config uniformly — the LLM provider is an implementation detail.
  </action>
  <verify>Config logs correct baseURL+model for each provider when switched</verify>
  <done>Config supports 3 providers, switching works by changing LLM_PROVIDER env var</done>
</task>

<task type="auto">
  <name>Task 2: Integration Test — Real browser + Ollama</name>
  <files>tests/integration.test.ts, tests/pages/example-form.html</files>
  <action>
    Create `tests/pages/example-form.html` — a static HTML page with:
    - A form with: name input, email input, submit button
    - A visible heading, a paragraph, a link
    - A checkbox, a select dropdown
    - data-testid attributes on key elements
    
    Create `tests/integration.test.ts`:
    - Load the example-form.html (serve locally or use file://)
    - Test 1: ai('click the submit button', { page }) — with Ollama
    - Test 2: ai('type "Carlos" in the name field', { page })
    - Test 3: ai('assert the heading is visible', { page, type: 'assert' })
    - Test 4: ai('query the email field value', { page, type: 'query' })
    - Each test uses Playwright's page.goto() + waits for load
    
    IMPORTANT: These are REAL LLM calls. Mark them as integration tests with a SKIP mechanism for CI that doesn't have API keys. Use test.skip() if OLLAMA_API_KEY is not set.
  </action>
  <verify>bun test tests/integration.test.ts passes with real Ollama API call</verify>
  <done>Integration tests pass with gemma4:31b via Ollama Cloud</done>
</task>

<task type="auto">
  <name>Task 3: Test with MiniMax as second provider</name>
  <files>tests/integration-minimax.test.ts</files>
  <action>
    Duplicate the integration test structure as `tests/integration-minimax.test.ts`
    Run with MINIMAX_API_KEY + LLM_PROVIDER=minimax
    Compare results: same DOM, same instructions → should get same selector decisions
    Both providers should work without code changes (only env vars)
  </action>
  <verify>Same tests pass with MiniMax-M2.7 API key</verify>
  <done>Both ollama and minimax work for the same test cases</done>
</task>

<task type="auto">
  <name>Task 4: Error handling — LLM timeout + API errors</name>
  <files>src/llm-client.ts (add retry/timeouts), tests/error-handling.test.ts</files>
  <action>
    Enhance llm-client.ts:
    - Configurable timeout per request (default 30s, configurable via LLM_TIMEOUT_MS)
    - Retry on 429/500/502/503/timeout (max 2 retries, exponential backoff 500ms/1s)
    - Clear error messages: "LLM timeout after 30s" vs "Invalid API key"
    - Stream responses: handle partial JSON if streaming
    
    Create `tests/error-handling.test.ts`:
    - Test: invalid API key → clear "401 Unauthorized" error message
    - Test: timeout → "LLM request timeout after 30s"
    - Test: network error → informative error
    - Test: malformed LLM response → "Failed to parse LLM response: ..."
    
    Mock these with a mock server or by using a wrong API key
  </action>
  <verify>Error messages are clear and actionable; no raw stack traces to users</verify>
  <done>Error handling is robust and user-friendly</done>
</task>

<task type="auto">
  <name>Task 5: README + Quickstart</name>
  <files>README.md</files>
  <action>
    Create `README.md`:
    - One-paragraph description
    - Quickstart: npm install, set .env, write first test
    - Example usage code block
    - Provider configuration (Ollama Cloud, MiniMax, OpenAI, Local Ollama)
    - Environment variables table
    - Architecture diagram (simple text-based)
    - Link to GSD plans
  </action>
  <verify>README is complete, accurate, and copy-pasteable to get started</verify>
  <done>README.md exists with setup instructions, examples, and architecture</done>
</task>

## Phase Verification Checklist
- [ ] Provider switching works — OLLAMA_MODEL=gemma4:31b works
- [ ] Integration tests pass with real Ollama Cloud gemma4:31b
- [ ] Same tests pass with MiniMax (same assertions, same behavior)
- [ ] Error handling: clear messages for bad key / timeout / network error
- [ ] README.md complete with quickstart
- [ ] All Phase 1 tests still pass (regression check)